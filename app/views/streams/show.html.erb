<p id="notice"><%= notice %></p>

<%= link_to 'Edit', edit_stream_path(@stream) %> |
<%= link_to 'Back', streams_path %>

<h3> Category </h3>
<%= @stream.category %>

<h3> Posts </h3>

<%= link_to "New Post", new_stream_post_path(@stream.id) %>

<%# @posts.each do |post| %>



<%# end %>


